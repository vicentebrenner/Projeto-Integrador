# --- CONFIGURAÇÃO DO BANCO DE DADOS ---
spring.datasource.url=jdbc:postgresql://localhost:5432/music_makers_db
spring.datasource.username=postgres
# Verifique se esta é a senha correta do seu banco de dados PostgreSQL!
spring.datasource.password=root

# --- CONFIGURAÇÃO DO JPA/HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CONFIGURAÇÃO DE E-MAIL (ETHEREAL) ---
spring.mail.host=smtp.ethereal.email
spring.mail.port=587
spring.mail.username=rosina.emard72@ethereal.email
spring.mail.password=NdVwpa2AUsNbYuS6ZN
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- CONFIGURAÇÃO DE SEGURANÇA (JWT) ---
# Chave secreta para JWT (use um valor longo e aleatório, mas para o teste este serve)
jwt.secret=SuaChaveSuperSecretaDePeloMenos256BitsParaSerSegura
```


    

